# Host packages confirmed so far (Debian/Ubuntu names).
# Not a runner. Artifact flow is still incomplete.

clang-14
clang++-14
cmake
ninja-build
g++
python3
python3-numpy
python3-pip
util-linux          # setarch
onnx                # python3 -m pip install onnx
onnxruntime         # python3 -m pip install onnxruntime  (ρ_static)
# label attack train: conda env statistical (torch + CUDA)

# victims/tvm/compile.sh uses /usr/bin/python3 when present
# (conda libstdc++ is often too old for in-tree libtvm_ffi).
